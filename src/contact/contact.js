import './contact.css';

export const contactPage = (() => {
	const contact = document.createElement('div');
	contact.classList.add('contact');

	// temp
	contact.innerText = 'Contact Content';

	return contact;
});

